import { Box, Image, Heading, Text, Flex } from "@chakra-ui/react"

type TechnologyData = {
    images: any, 
}

const Technology = ({ images } : TechnologyData) => {
    return(
        <Box>

        </Box>
    );
}

export default Technology 